import 'package:flutter/material.dart';

class DiscoveryFilmDetailHeader extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Stack(
        children: <Widget>[
          
        ],
      ),
    );
  }
}
